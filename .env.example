# Optional: enables Claude for normalization, vision, and structured analysis.
# Without this key, the app uses a deterministic demo analysis (safe for UI review).
ANTHROPIC_API_KEY=

# Optional: server-side transcription for uploaded audio (Whisper).
# Without this key, use in-browser speech or type/paste transcript.
OPENAI_API_KEY=
